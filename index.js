window.addEventListener('load', () => {
    // capture the first button property in a variable
    const notifyButton = document.getElementsByTagName('button')[0];
    
    // notifyButton.innerHTML = "No, Don't Notify Me";
    // if () {} else if () {} else {}
    
});